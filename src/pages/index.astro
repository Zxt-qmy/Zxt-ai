---
import Layout from '~/layouts/PageLayout.astro';
import Hero from '~/components/widgets/Hero.astro';
import Features2 from '~/components/widgets/Features2.astro';

const metadata = {
  title: 'Zxt AI实验室',
  ignoreTitleTemplate: true,
};
---

<Layout metadata={metadata}>
  <!-- 1. 顶部标题区 (视觉升级版：大字号+极光背景) -->
  <Hero
    actions={[
      {
        variant: 'primary',
        text: '立即使用',
        href: '#projects', 
        icon: 'tabler:rocket',
      },
    ]}
  >
    <Fragment slot="title">
      欢迎来到 
      <br class="block sm:hidden" />
      <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 via-purple-500 to-indigo-400 font-extrabold text-5xl md:text-7xl tracking-tighter">
        Zxt AI 实验室
      </span>
    </Fragment>

    <Fragment slot="subtitle">
      <span class="hidden sm:inline text-lg text-gray-600 dark:text-gray-400">
        这里是我探索 AI 技术的个人空间。收录了我搭建的实用工具以及一些有趣的 AI 实验项目。
      </span>
    </Fragment>

    <!-- 背景光晕特效  -->
    <Fragment slot="bg">
      <div class="absolute inset-0 bg-white dark:bg-[#030620]"></div>
      <div class="absolute top-0 left-1/2 -translate-x-1/2 w-full h-[500px] -z-10 pointer-events-none opacity-40 dark:opacity-20">
        <div class="absolute top-[-10%] left-1/2 -translate-x-1/2 w-[600px] h-[600px] bg-purple-500/30 blur-[100px] rounded-full mix-blend-multiply dark:mix-blend-screen"></div>
        <div class="absolute top-[-10%] left-[20%] w-[500px] h-[500px] bg-blue-500/20 blur-[80px] rounded-full mix-blend-multiply dark:mix-blend-screen"></div>
      </div>
    </Fragment>
  </Hero>

  <!-- 2. 我的项目展示区（带筛选功能） -->
  <section id="projects" class="max-w-6xl mx-auto px-4 lg:px-8 py-8 md:py-12">
    <!-- 标题区域 -->
    <div class="text-center mb-8">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-white mb-3">
        我的项目
      </h2>
      <p class="text-lg text-gray-600 dark:text-gray-400">
        选择一个工具来启动应用
      </p>
    </div>

    <!-- 筛选标签栏 -->
    <div class="flex flex-wrap justify-center gap-2 md:gap-3 mb-10 md:mb-12">
      {['全部', '文本与对话', '图像与视频', '语音与音频', '实用工具', '数据分析'].map((category) => (
        <button
          class={`px-4 py-2 rounded-full text-sm md:text-base font-medium transition-all duration-200 ${category === '全部' 
            ? 'bg-blue-600 text-white shadow-md' 
            : 'bg-white dark:bg-gray-800 text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 border border-gray-200 dark:border-gray-700'}`}
          data-filter={category === '全部' ? 'all' : category}
        >
          {category}
        </button>
      ))}
    </div>

    <!-- 项目卡片容器 -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <!-- 项目1: Markdown 灵感笔记 -->
      <div 
        class="group bg-white dark:bg-gray-800 rounded-2xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 border border-gray-100 dark:border-gray-700"
        data-category="文本与对话"
      >
        <div class="flex items-start mb-5">
          <div class="w-12 h-12 flex items-center justify-center bg-blue-100 dark:bg-blue-900/30 rounded-full p-2.5 mr-4">
            <div class="w-7 h-7 text-blue-600 dark:text-blue-400">
              <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M12 3H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path>
                <path d="M18.375 2.625a1 1 0 0 1 3 3l-9.013 9.014a2 2 0 0 1-.853.505l-2.873.84a.5.5 0 0 1-.62-.62l.84-2.873a2 2 0 0 1 .506-.852z"></path>
              </svg>
            </div>
          </div>
          <div>
            <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">Markdown 灵感笔记</h3>
            <span class="inline-block px-3 py-1 text-xs font-medium bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 rounded-full">
              文本与对话
            </span>
          </div>
        </div>
        <p class="text-gray-600 dark:text-gray-400 mb-6 leading-relaxed">
          简约而不简单的在线 Markdown 编辑器。支持实时预览与极简模式，为您的创作提供无干扰的沉浸式体验。
        </p>
        <a 
          href="#" 
          class="inline-flex items-center text-blue-600 dark:text-blue-400 font-medium hover:text-blue-700 dark:hover:text-blue-300 transition-colors"
        >
          立即使用
          <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
          </svg>
        </a>
      </div>

      <!-- 项目2: 智能体测试平台 -->
      <div 
        class="group bg-white dark:bg-gray-800 rounded-2xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 border border-gray-100 dark:border-gray-700"
        data-category="文本与对话"
      >
        <div class="flex items-start mb-5">
          <div class="w-12 h-12 flex items-center justify-center bg-purple-100 dark:bg-purple-900/30 rounded-full p-2.5 mr-4">
            <div class="w-7 h-7 text-purple-600 dark:text-purple-400">
              <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M12 8V4H8"></path>
                <rect width="16" height="12" x="4" y="8" rx="2"></rect>
                <path d="M2 14h2"></path>
                <path d="M20 14h2"></path>
                <path d="M15 13v2"></path>
                <path d="M9 13v2"></path>
              </svg>
            </div>
          </div>
          <div>
            <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">智能体测试平台</h3>
            <span class="inline-block px-3 py-1 text-xs font-medium bg-purple-100 dark:bg-purple-900/30 text-purple-700 dark:text-purple-300 rounded-full">
              文本与对话
            </span>
          </div>
        </div>
        <p class="text-gray-600 dark:text-gray-400 mb-6 leading-relaxed">
          专业的对话智能体调试工作台。支持自主配置密钥与应用 ID，为开发者提供灵活的智能体交互测试环境。
        </p>
        <a 
          href="#" 
          class="inline-flex items-center text-purple-600 dark:text-purple-400 font-medium hover:text-purple-700 dark:hover:text-purple-300 transition-colors"
        >
          立即使用
          <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
          </svg>
        </a>
      </div>

      <!-- 项目3: 更多工具开发中... -->
      <div 
        class="group bg-white dark:bg-gray-800 rounded-2xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 border border-gray-100 dark:border-gray-700"
        data-category="实用工具"
      >
        <div class="flex items-start mb-5">
          <div class="w-12 h-12 flex items-center justify-center bg-indigo-100 dark:bg-indigo-900/30 rounded-full p-2.5 mr-4">
            <div class="w-7 h-7 text-indigo-600 dark:text-indigo-400">
              <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5"></path>
                <path d="M9 18h6"></path>
                <path d="M10 22h4"></path>
              </svg>
            </div>
          </div>
          <div>
            <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">更多工具开发中...</h3>
            <span class="inline-block px-3 py-1 text-xs font-medium bg-indigo-100 dark:bg-indigo-900/30 text-indigo-700 dark:text-indigo-300 rounded-full">
              实用工具
            </span>
          </div>
        </div>
        <p class="text-gray-600 dark:text-gray-400 mb-6 leading-relaxed">
          敬请期待更多有趣的 AI 实验项目上线。
        </p>
        <div class="text-gray-400 dark:text-gray-500 font-medium">
          敬请期待
        </div>
      </div>

      <!-- 新增示例项目 -->
      <div 
        class="group bg-white dark:bg-gray-800 rounded-2xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 border border-gray-100 dark:border-gray-700"
        data-category="图像与视频"
      >
        <div class="flex items-start mb-5">
          <div class="w-12 h-12 flex items-center justify-center bg-green-100 dark:bg-green-900/30 rounded-full p-2.5 mr-4">
            <div class="w-7 h-7 text-green-600 dark:text-green-400">
              <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect width="18" height="18" x="3" y="3" rx="2"></rect>
                <circle cx="9" cy="9" r="2"></circle>
                <path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"></path>
              </svg>
            </div>
          </div>
          <div>
            <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">AI 图片生成器</h3>
            <span class="inline-block px-3 py-1 text-xs font-medium bg-green-100 dark:bg-green-900/30 text-green-700 dark:text-green-300 rounded-full">
              图像与视频
            </span>
          </div>
        </div>
        <p class="text-gray-600 dark:text-gray-400 mb-6 leading-relaxed">
          基于 AI 的创意图片生成工具，支持多种艺术风格，快速将文字描述转化为视觉图像。
        </p>
        <a 
          href="#" 
          class="inline-flex items-center text-green-600 dark:text-green-400 font-medium hover:text-green-700 dark:hover:text-green-300 transition-colors"
        >
          立即使用
          <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
          </svg>
        </a>
      </div>
    </div>

    <!-- 筛选功能的 JavaScript -->
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        const filterButtons = document.querySelectorAll('[data-filter]');
        const projectCards = document.querySelectorAll('[data-category]');
        
        filterButtons.forEach(button => {
          button.addEventListener('click', function() {
            // 更新按钮状态
            filterButtons.forEach(btn => {
              btn.classList.remove('bg-blue-600', 'text-white', 'shadow-md');
              btn.classList.add('bg-white', 'dark:bg-gray-800', 'text-gray-700', 'dark:text-gray-300', 'border', 'border-gray-200', 'dark:border-gray-700');
            });
            
            this.classList.remove('bg-white', 'dark:bg-gray-800', 'text-gray-700', 'dark:text-gray-300', 'border', 'border-gray-200', 'dark:border-gray-700');
            this.classList.add('bg-blue-600', 'text-white', 'shadow-md');
            
            // 筛选项目
            const filter = this.getAttribute('data-filter');
            
            projectCards.forEach(card => {
              if (filter === 'all' || card.getAttribute('data-category') === filter) {
                card.style.display = 'block';
                setTimeout(() => {
                  card.style.opacity = '1';
                  card.style.transform = 'translateY(0)';
                }, 10);
              } else {
                card.style.opacity = '0';
                card.style.transform = 'translateY(10px)';
                setTimeout(() => {
                  card.style.display = 'none';
                }, 300);
              }
            });
          });
        });
        
        // 初始状态：显示所有项目
        projectCards.forEach(card => {
          card.style.transition = 'opacity 0.3s ease, transform 0.3s ease';
        });
      });
    </script>
    
    <style>
      [data-category] {
        transition: opacity 0.3s ease, transform 0.3s ease;
      }
    </style>
  </section>
</Layout>