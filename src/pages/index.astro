---
import Layout from '~/layouts/PageLayout.astro';
import Hero from '~/components/widgets/Hero.astro';

const metadata = {
  title: 'Zxt AI 实验室 - 探索我的 AI 工具箱',
  ignoreTitleTemplate: true,
};
---

<Layout metadata={metadata}>
  <!-- 1. 顶部标题区 -->
  <Hero actions={[]}>
    <Fragment slot="title">
      <span class="block text-lg md:text-xl text-blue-600 dark:text-blue-400 mb-2 font-bold tracking-wide uppercase">
        AI Productivity Tools
      </span>
      欢迎来到 
      <br class="block sm:hidden" />
      <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 via-purple-500 to-indigo-400 font-extrabold text-5xl md:text-7xl tracking-tighter">
        Zxt AI 实验室
      </span>
    </Fragment>

    <Fragment slot="subtitle">
      <span class="hidden sm:inline text-lg text-gray-600 dark:text-gray-400">
        这里是我探索 AI 技术的个人空间。收录了我搭建的实用工具以及一些有趣的 AI 实验项目。
      </span>
    </Fragment>

    <!-- 背景光晕特效 -->
    <Fragment slot="bg">
      <div class="absolute inset-0 bg-white dark:bg-[#030620]"></div>
      <div class="absolute top-0 left-1/2 -translate-x-1/2 w-full h-[500px] -z-10 pointer-events-none opacity-40 dark:opacity-20">
        <div class="absolute top-[-10%] left-1/2 -translate-x-1/2 w-[600px] h-[600px] bg-purple-500/30 blur-[100px] rounded-full mix-blend-multiply dark:mix-blend-screen"></div>
        <div class="absolute top-[-10%] left-[20%] w-[500px] h-[500px] bg-blue-500/20 blur-[80px] rounded-full mix-blend-multiply dark:mix-blend-screen"></div>
      </div>
    </Fragment>
  </Hero>

  <!-- 2. 我的项目展示区 -->
  <section id="projects" class="max-w-6xl mx-auto px-4 lg:px-8 py-2 md:py-6 mt-[-20px] mb-20 relative z-10">
    
    <!-- 标题与筛选栏容器：使用 Flex 布局实现左右对齐 -->
    <div class="flex flex-col md:flex-row md:items-end justify-between mb-8 gap-4">
      
      <!-- 左边：标题和副标题 -->
      <div class="text-left">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-white mb-2">
          我的项目
        </h2>
        <p class="text-gray-600 dark:text-gray-400">
          选择一个工具来启动应用
        </p>
      </div>

      <!-- 右边：筛选按钮 (补全了你截图里的所有分类) -->
      <div class="flex flex-wrap gap-2">
        {['全部', '文本与对话', '图像与视频', '语音与音频', '实用工具', '数据分析'].map((category) => (
          <button
            class={`px-4 py-2 rounded-full text-sm font-medium transition-all duration-200 ${category === '全部' 
              ? 'bg-blue-600 text-white shadow-md' 
              : 'bg-white dark:bg-gray-800 text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 border border-gray-200 dark:border-gray-700'}`}
            data-filter={category === '全部' ? 'all' : category}
          >
            {category}
          </button>
        ))}
      </div>
    </div>

    <!-- 项目卡片网格 -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5">
      
      <!-- 卡片 1: Markdown -->
      <div class="group bg-white dark:bg-gray-800 rounded-2xl p-5 shadow-sm hover:shadow-xl transition-all duration-300 border border-gray-100 dark:border-gray-700" data-category="文本与对话">
        <div class="flex items-start mb-4">
          <div class="w-10 h-10 flex items-center justify-center bg-blue-100 dark:bg-blue-900/30 rounded-full mr-3">
            <svg class="w-6 h-6 text-blue-600 dark:text-blue-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path><path d="M18.375 2.625a1 1 0 0 1 3 3l-9.013 9.014a2 2 0 0 1-.853.505l-2.873.84a.5.5 0 0 1-.62-.62l.84-2.873a2 2 0 0 1 .506-.852z"></path></svg>
          </div>
          <div>
            <h3 class="text-lg font-bold text-gray-900 dark:text-white">Markdown 灵感笔记</h3>
            <span class="text-xs text-blue-600 dark:text-blue-400 bg-blue-50 dark:bg-blue-900/20 px-2 py-0.5 rounded-full">文本与对话</span>
          </div>
        </div>
        <p class="text-sm text-gray-600 dark:text-gray-400 mb-4 line-clamp-2">
          简约而不简单的在线 Markdown 编辑器。支持实时预览与极简模式。
        </p>
        <a href="#" class="inline-flex items-center text-sm font-medium text-blue-600 hover:text-blue-700 dark:text-blue-400">
          立即使用 <svg class="w-4 h-4 ml-1" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
      </div>

      <!-- 卡片 2: 智能体平台 -->
      <div class="group bg-white dark:bg-gray-800 rounded-2xl p-5 shadow-sm hover:shadow-xl transition-all duration-300 border border-gray-100 dark:border-gray-700" data-category="文本与对话">
        <div class="flex items-start mb-4">
          <div class="w-10 h-10 flex items-center justify-center bg-purple-100 dark:bg-purple-900/30 rounded-full mr-3">
            <svg class="w-6 h-6 text-purple-600 dark:text-purple-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 8V4H8"/><rect width="16" height="12" x="4" y="8" rx="2"/><path d="M2 14h2"/><path d="M20 14h2"/><path d="M15 13v2"/><path d="M9 13v2"/></svg>
          </div>
          <div>
            <h3 class="text-lg font-bold text-gray-900 dark:text-white">智能体测试平台</h3>
            <span class="text-xs text-purple-600 dark:text-purple-400 bg-purple-50 dark:bg-purple-900/20 px-2 py-0.5 rounded-full">文本与对话</span>
          </div>
        </div>
        <p class="text-sm text-gray-600 dark:text-gray-400 mb-4 line-clamp-2">
          专业的对话智能体调试工作台。支持自主配置密钥与应用 ID。
        </p>
        <a href="#" class="inline-flex items-center text-sm font-medium text-purple-600 hover:text-purple-700 dark:text-purple-400">
          立即使用 <svg class="w-4 h-4 ml-1" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
      </div>

      <!-- 卡片 3: AI 图片生成器 -->
      <div class="group bg-white dark:bg-gray-800 rounded-2xl p-5 shadow-sm hover:shadow-xl transition-all duration-300 border border-gray-100 dark:border-gray-700" data-category="图像与视频">
        <div class="flex items-start mb-4">
          <div class="w-10 h-10 flex items-center justify-center bg-green-100 dark:bg-green-900/30 rounded-full mr-3">
            <svg class="w-6 h-6 text-green-600 dark:text-green-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="18" x="3" y="3" rx="2"/><circle cx="9" cy="9" r="2"/><path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"/></svg>
          </div>
          <div>
            <h3 class="text-lg font-bold text-gray-900 dark:text-white">AI 图片生成器</h3>
            <span class="text-xs text-green-600 dark:text-green-400 bg-green-50 dark:bg-green-900/20 px-2 py-0.5 rounded-full">图像与视频</span>
          </div>
        </div>
        <p class="text-sm text-gray-600 dark:text-gray-400 mb-4 line-clamp-2">
          基于 AI 的创意图片生成工具，快速将文字描述转化为视觉图像。
        </p>
        <a href="#" class="inline-flex items-center text-sm font-medium text-green-600 hover:text-green-700 dark:text-green-400">
          立即使用 <svg class="w-4 h-4 ml-1" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
      </div>

      <!-- 卡片 4: 更多工具 (占位符) -->
      <div class="group bg-gray-50 dark:bg-gray-800/50 rounded-2xl p-5 shadow-none border border-dashed border-gray-300 dark:border-gray-700 flex flex-col justify-center items-center text-center opacity-70" data-category="实用工具">
        <div class="w-10 h-10 flex items-center justify-center bg-gray-200 dark:bg-gray-700 rounded-full mb-3">
          <svg class="w-6 h-6 text-gray-500 dark:text-gray-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 5v14M5 12h14"/></svg>
        </div>
        <h3 class="text-lg font-bold text-gray-900 dark:text-white">更多工具开发中...</h3>
        <p class="text-xs text-gray-500 mt-1">敬请期待新项目上线</p>
      </div>

    </div>

    <!-- 筛选逻辑脚本 -->
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        const btns = document.querySelectorAll('[data-filter]');
        const cards = document.querySelectorAll('[data-category]');
        btns.forEach(btn => btn.addEventListener('click', () => {
          // 重置所有按钮样式
          btns.forEach(b => b.className = b.className.replace('bg-blue-600 text-white shadow-md', 'bg-white dark:bg-gray-800 text-gray-600 dark:text-gray-300 border border-gray-200 dark:border-gray-700'));
          // 激活当前按钮
          btn.className = btn.className.replace('bg-white dark:bg-gray-800 text-gray-600 dark:text-gray-300 border border-gray-200 dark:border-gray-700', 'bg-blue-600 text-white shadow-md');
          
          const f = btn.getAttribute('data-filter');
          cards.forEach(c => {
            // 显示/隐藏逻辑
            if (f === 'all' || c.getAttribute('data-category') === f) {
              c.style.display = 'block';
            } else {
              c.style.display = 'none';
            }
          });
        }));
      });
    </script>
  </section>
</Layout>